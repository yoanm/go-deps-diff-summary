coverage:
    range: "80...100"

flags:
    nightly:
        joined: false

comment:
    show_carryforward_flags: true

github_checks:
    annotations: true

ignore:
  - "testing/**" # Not covered as for test purpose only
